# Database
DB_HOST=db
DB_USER=branch
DB_PASSWORD=branchpass
DB_NAME=branchloans
DB_PORT=5432

# App
PORT=8000
NODE_ENV=development
LOG_LEVEL=debug

# Image (used by prod compose)
IMAGE_NAME=branch-loan-api
IMAGE_TAG=latest

# GitHub / registry (for CI)
# Set GHCR_USERNAME and GHCR_TOKEN in GitHub Actions secrets
